---
title = "Function (computer science)"
authors = [ "kyo" ]
---

A function is a set of instructions in a [program]($p/program) which may take
some input ([arguments]($a/argument)) and produce some output (a *return
value*). A function may also have *side effects* on program or machine state
unrelated to the inputs or outputs.
